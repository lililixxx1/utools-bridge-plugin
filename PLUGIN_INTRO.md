# 插件手机同步 — 插件介绍

> 备好两段：① 列表卡片一行短介绍 ② 详情页正文。提交 uTools 开发者后台时分别贴入。
> 对照市场惯例（「本地搜索」「易翻翻译」）：正文用加粗【】小节标题、混用编号/项目列表、按 简介→能力→用法→说明 展开。

---

## ① 列表卡片短介绍（plugin.json description）

```
手机访问 PC 端 uTools - 待办/笔记/剪贴板双向同步，同 WiFi 浏览器打开即用，可加到主屏幕当 App
```

句式对齐市场同类（「极好用的剪贴板增强工具 - 历史记录查找、合并复制、执行粘贴、悬浮中转」）：
动词开头 + 能力点（用 `/` 分隔）+ 一句话卖点（免装 App / 同 WiFi / 打开即用）。
关键词全覆盖：手机、PC、uTools、待办、笔记、剪贴板、同步、WiFi、App。

---

## ② 详情页正文（介绍）

```
把 PC 端 uTools 的待办、笔记、剪贴板搬到手机。无需装 App，同一 WiFi 下手机浏览器打开一个地址即用，支持添加到主屏幕像 App 一样启动。

【能做什么】
· 待办：按分组查看、搜索、新建、勾选完成
· 笔记：全文搜索、阅读详情、新建笔记
· 剪贴板：历史记录搜索；把文字/图片/文件复制进 PC 剪贴板；预览剪贴板里的图片；下载剪贴板里的文件；手机上传图片或小文件到 PC 剪贴板

【如何使用】
1. uTools 开启本地 MCP：设置 → AI Agent 连接 → 启用 MCP，复制弹出的配置
2. 主搜索框输入「插件手机同步」打开本插件，把配置粘贴进「MCP 配置」卡片，点「应用」
3. 点「启动桥接」，手机连同同一 WiFi，浏览器打开控制台显示的「访问地址」即可
4. （可选）手机浏览器「添加到主屏幕」，像 App 一样直达

【安全】
首次启动自动生成随机访问密钥，默认开启鉴权——局域网内其他人无法读取你的剪贴板（可能含密码）、笔记、待办。想换密钥点「换一个」；密钥只存在本机，不上传、不走 uTools 云同步。

【特点】
· 不占手机存储：纯网页 PWA，不装 App、不联网，数据只存在你 PC 上
· 打开即用：手机访问一个地址即可，无需手动填 IP、端口、密钥
· 可当 App 用：添加到主屏幕后是独立入口，启动直达、无浏览器边框
```

---

## 文案要点说明

- **列表卡片卖点三连**："待办/笔记/剪贴板双向同步" + "同 WiFi 打开即用" + "可加到主屏幕当 App"——分别覆盖 功能广度 / 0 门槛 / 长期价值，对应这类插件用户最在意的三点。
- **【能做什么】把剪贴板能力点写全**：剪贴板是本插件能力最丰富的模块（搜索、回写、图片预览、文件下载、手机上传），原稿只写了"搜索+复制"会严重低估卖点。
- **【如何使用】第 1 步点明入口路径**：uTools 的 MCP 开关藏在「设置 → AI Agent 连接」，不写出来用户找不到。
- **【安全】单独成块**：主动点明"剪贴板可能含密码 + 默认鉴权 + 密钥不上云"——是用户最大的顾虑，不写出来会流失安装。
- **【特点】讲用户利益不讲技术**："界面和数据同源"对普通用户是黑话，换成"无需手动填 IP/端口/密钥"才是用户能感知的卖点。
- **不加【常见问题】/更新日志**：对齐「本地搜索」式收尾（正文结束即止），避免无真实更新内容时硬编。
